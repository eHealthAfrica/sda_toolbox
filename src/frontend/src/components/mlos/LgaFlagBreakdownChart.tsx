// Superseded by FlagBreakdownChart.tsx, which generalizes this LGA-only,
// non-drilling chart into one component shared across the state/LGA/ward
// levels (MlosQcPage.tsx now starts at state and drills down, now that
// /qc/validation accepts settlements spanning multiple states — see
// api/client.ts::submitMlosQC). Stubbed rather than deleted — no file-delete
// tool available in this environment; nothing imports this file anymore.
export {}
